<?xml version="1.0" encoding="UTF-8" ?>
<?xaml-comp compile="true" ?>
<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Style TargetType="Page">
        <Setter Property="BackgroundColor" Value="{StaticResource DeepIceBackground}" />
    </Style>
    
    <Style TargetType="Label">
        <Setter Property="TextColor" Value="{StaticResource TextPrimary}" />
        <Setter Property="FontFamily" Value="OpenSansRegular" />
    </Style>

    <Style x:Key="NeonButtonStyle" TargetType="Button">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="TextColor" Value="{StaticResource NeonCyan}" />
        <Setter Property="BorderColor" Value="{StaticResource NeonCyan}" />
        <Setter Property="BorderWidth" Value="2" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="14,10" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Style.Triggers>
            <Trigger TargetType="Button" Property="IsPressed" Value="True">
                <Setter Property="BackgroundColor" Value="{StaticResource NeonCyan}" />
                <Setter Property="TextColor" Value="#000000" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="GlassCardStyle" TargetType="Frame">
        <Setter Property="BackgroundColor" Value="{StaticResource GlassSurface}" />
        <Setter Property="CornerRadius" Value="16" />
        <Setter Property="BorderColor" Value="{StaticResource NeonCyan}" />
        <Setter Property="HasShadow" Value="True" />
        <Setter Property="Padding" Value="16" /> 
    </Style>

    <Style x:Key="SafetyWidgetStyle" TargetType="Border">
        <Setter Property="Stroke" Value="{StaticResource NeonMagenta}" />
        <Setter Property="StrokeThickness" Value="2" />
        <Setter Property="BackgroundColor" Value="#33FF0099" />
        <Setter Property="StrokeShape" Value="RoundRectangle 12" />
        <Setter Property="Padding" Value="12" />
    </Style>

</ResourceDictionary>
